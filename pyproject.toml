[project]
name = "aeon-ss-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "broo-helper",
    "ipykernel>=7.3.0",
    "kilosort>=4.1.7",
    "spikeinterface>=0.104.8",
]

[build-system]
requires = ["setuptools>=78.0.2"]
build-backend = "setuptools.build_meta"

[tool.uv.sources]
spikeinterface = { path = "../spikeinterface" }
broo-helper = { path = "../broo_helper" }
